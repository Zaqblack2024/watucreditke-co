FROM node:20-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:stable-alpine as prod
COPY --from=build /app/dist /usr/share/nginx/html
# Add nginx config if needed (for SPA fallback)
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]